name: Arrêter le contrôle natif redondant du cours 06

on:
  push:
    paths:
      - .github/workflows/build-course6-apt.yml

concurrency:
  group: build-course6-apt-${{ github.ref }}
  cancel-in-progress: true

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Le contrôle du cours 06 est assuré par build-course6.yml."
