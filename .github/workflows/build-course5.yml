name: Arrêter le contrôle du cours 05

on:
  push:
    paths:
      - .github/workflows/build-course5.yml

concurrency:
  group: build-course5-${{ github.ref }}
  cancel-in-progress: true

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ancien contrôle du cours 05 neutralisé."
