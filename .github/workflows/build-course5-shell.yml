name: Arrêter le contrôle autonome du cours 05

on:
  push:
    paths:
      - .github/workflows/build-course5-shell.yml

concurrency:
  group: build-course5-shell-${{ github.ref }}
  cancel-in-progress: true

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ancien contrôle autonome du cours 05 neutralisé."
