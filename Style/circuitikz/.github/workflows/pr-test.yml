name: PR test compile

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  test-compile:
    name: Test compile
    if: github.repository == 'circuitikz/circuitikz'
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            texlive-pictures \
            texlive-science \
            texlive-plain-generic \
            texlive-latex-extra \
            lmodern \
            pandoc

      - name: Test compile
        run: make test-compile

